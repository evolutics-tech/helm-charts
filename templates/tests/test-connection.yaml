apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "evolutics.fullname" . }}-test-connection"
  labels:
    {{- include "evolutics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "evolutics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
